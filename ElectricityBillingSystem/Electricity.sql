create Database Electricity_Billing_System;
use Electricity_Billing_System;

create table Signup(meter_no varchar(20), employer_id varchar(10), username varchar(20), name varchar(20), password varchar(20), usertype varchar(20));
select * from Signup;